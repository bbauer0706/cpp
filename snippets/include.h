#ifndef INCLUDE_H 
#define INCLUDE_H

#include <iostream>     // Standard input and output
#include <iomanip>      // For manipulating output format (e.g., setprecision)
#include <string>       // For strings
#include <vector>       // For dynamic arrays
#include <array>        // For fixed-size arrays
#include <deque>        // Double-ended queue
#include <list>         // Linked list
#include <map>          // Associative container (key-value pairs)
#include <unordered_map>// Faster associative container (key-value pairs, no order)
#include <set>          // Ordered set
#include <unordered_set>// Unordered set
#include <stack>        // Stack data structure
#include <queue>        // Queue and priority queue
#include <algorithm>    // For algorithms like sort, find, etc.
#include <cmath>        // For math functions like pow, sqrt, sin, etc.
#include <numeric>      // For numeric operations like accumulate, gcd, etc.
#include <fstream>      // For file I/O
#include <sstream>      // For string stream manipulation
#include <bitset>       // For binary representation
#include <random>       // For generating random numbers
#include <chrono>       // For measuring time (e.g., benchmarking)
#include <functional>   // For function objects, bind, etc.
#include <tuple>        // For tuple handling
#include <typeinfo>     // For type information
#include <stdexcept>    // For standard exceptions
#include <cassert>      // For debugging with assert
#include <cctype>       // For character functions (e.g., isalpha, isdigit)
#include <cstring>      // For C-style string functions
#include <utility>      // For utility functions like pair, swap
#include <iterator>     // For iterators and iterator utilities
#include <limits>       // For numerical limits of data types

using namespace std;

#endif 
